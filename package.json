{
  "name": "graph-algorithms",
  "version": "0.1.0",
  "description": "",
  "main": ".webpack.config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && webpack --mode development --entry ./dist/index.js --output ./bundle.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^4.17.1",
    "webpack-cli": "^3.1.0"
  },
  "dependencies": {
    "velocity-animate": "^2.0.5"
  }
}
